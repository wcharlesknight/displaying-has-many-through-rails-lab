
<% @doctor.patients.each do |p|%>
    <% @doctor.appointments.each do |a|%>
        <%= a.appointment_datetime.strftime("%B %d, %Y at %H:%M")%>
        <%= link_to p.name, patient_path(p.id) %><br>
    <%end%>
<%end%>